//for (let i = 2; i < 8; i += 2) {
    let colours = ["Blue", "Red", "Orange", "Pink"] ;
     {   console.log(colours);
        }
        // ***********************
        // Now display in reverse
        // ***********************
    colours.reverse(); 
        console.log(colours);
    